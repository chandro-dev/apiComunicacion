name: Basic CI

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  basic-check:
    runs-on:
      - self-hosted
      - linux
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Docker Compose file
        run: docker compose config
