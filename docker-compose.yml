services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${GHCR_IMAGE:-local/communication-api}:${IMAGE_TAG:-latest}
    container_name: communication-api
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"

